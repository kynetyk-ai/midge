[project]
name = "toybox"
version = "0.1.0"
description = "A small project for the midge harness to work on."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
